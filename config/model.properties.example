# 真实模型配置模板。
# 使用方式：复制为 config/model.properties，然后填写真实信息。

# 模型名称，例如：gpt-4.1、deepseek-chat、qwen-plus
model.name=your-model-name

# 模型 API 地址，例如：https://api.example.com/v1/chat/completions
model.url=https://api.example.com/v1/chat/completions

# 模型 API Key。不要把真实 Key 写入本模板。
model.apiKey=your-api-key
